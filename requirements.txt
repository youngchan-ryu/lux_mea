numpy>=2.0
opencv-python>=4.9
PyYAML>=6.0
rapidfuzz>=3.0
hgtk>=0.2
pillow>=10.0
requests>=2.31        # LLM endpoints: mine_vocab.py, speech.py --engine groq

# Speech. Only needed for the live demo; --no-voice and --sim run without them.
sounddevice>=0.4
soundfile>=0.12
mlx-whisper>=0.4        # Apple Silicon, Metal accelerated
# faster-whisper>=1.0   # portable fallback, select with --engine faster

# CLOVA Speech (--engine clova / clova-stream). Only the streaming engine needs
# these; grpcio-tools compiles nest.proto on first use. Needs the Basic
# long-sentence plan -- the Free plan does not serve the streaming API.
# grpcio>=1.60
# grpcio-tools>=1.60
# protobuf>=4.25

# The Helios DAC library ships in this directory as libHeliosLaserDAC.dylib and
# is found automatically. Set HELIOS_LIB only if you keep it elsewhere.
# Source: https://github.com/Grix/helios_dac
